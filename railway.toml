[build]
builder = "RAILPACK"

[deploy]
startCommand = "python watcher.py --verbose"
numReplicas = 1
requiredMountPath = "/data"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
overlapSeconds = 0
drainingSeconds = 10
