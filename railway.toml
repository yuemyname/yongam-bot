[build]
builder = "RAILPACK"

[deploy]
startCommand = "python service_entry.py --verbose"
numReplicas = 1
requiredMountPath = "/data"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
overlapSeconds = 0
drainingSeconds = 10
