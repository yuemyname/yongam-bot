# CGV 용산 IMAX Telegram 봇 운영·개발 안내

이 문서는 봇을 설치하고 운영하거나 코드를 테스트하는 사람을 위한 안내입니다. 알림 구독과 사용 방법은 [README.md](README.md)를 참고하세요.

## BotFather 설정

1. Telegram에서 `@BotFather`에게 `/newbot`을 보내 봇을 만듭니다.
2. 발급된 HTTP API Token을 안전하게 보관합니다.
3. `/setcommands`를 실행하고 아래 명령어를 등록합니다.

```text
start - 알림 구독
stop - 알림 해지
status - 구독 상태 확인
mode - 알림 종류 확인·변경
mode_all - 신규 오픈 + 잔여 좌석 받기
mode_open - 신규 오픈만 받기
mode_seats - 잔여 좌석만 받기
day - 알림 상영일 확인·변경
day_all - 모든 요일 상영분 받기
day_weekend - 토·일 상영분만 받기
seat - 잔여 좌석 대상 확인·변경
seat_all - 모든 A열 제외 좌석 받기
seat_sweet - 명당 좌석만 받기
count - 예매 가능 최소 좌석 확인·변경
count_1 - 1석부터 모두 받기
count_2 - 2석 이상 남았을 때만 받기
desc - 봇 설명과 사용 방법
coffee - 개발자에게 커피 후원
help - 사용법 보기
```

BotFather의 `/setdescription`에는 저장소에서 제공하는 한글·영문 소개문을 등록할 수 있습니다.

## 소개 페이지 (GitHub Pages)

`docs/index.html`이 검색에 노출되는 소개 페이지입니다. 저장소 README는 검색 결과에서 잘 잡히지 않기 때문에 별도로 둡니다.

**최초 1회 설정**

1. GitHub 저장소 → **Settings → Pages**
2. Source를 **Deploy from a branch**, 브랜치를 `main`, 폴더를 **`/docs`**로 지정하고 Save
3. 몇 분 뒤 `https://yuemyname.github.io/yongam-bot/` 로 열리는지 확인
4. 저장소 첫 화면 오른쪽 **About** 톱니바퀴에서 Description은 `[용아맥 알리미] CGV 용산아이파크몰 IMAX 예매 오픈 및 취소표 텔레그램 알림 봇 (yongam-bot)`, Website는 같은 GitHub Pages 소개 페이지 주소로 설정합니다. Telegram 주소를 Website에 넣으면 저장소에서 소개 페이지로 이어지는 검색 신호가 약해집니다.
5. About의 Topics에는 `telegram-bot`, `cgv`, `cgv-alert`, `imax`, `yongamaek`, `yongamaek-alert`, `yongsan-imax`, `movie-ticket`, `ticket-alert`, `python`, `railway`를 등록합니다.

**검색 등록**

| 서비스 | 등록 위치 | 제출할 것 |
|---|---|---|
| Google | [Search Console](https://search.google.com/search-console) | 사이트 주소 확인 후 `sitemap.xml` 제출, 색인 요청 |
| 네이버 | [서치어드바이저](https://searchadvisor.naver.com) | 사이트 등록 + 사이트맵 제출 |
| 다음 | [검색등록](https://register.search.daum.net) | 사이트 등록 |

한국어 영화·예매 검색은 네이버 비중이 커서 서치어드바이저 등록이 구글보다 효과가 클 수 있습니다.

색인까지는 보통 며칠에서 2~3주 걸립니다. 등록 직후 검색해도 안 나오는 것이 정상입니다.

**페이지를 고칠 때**

`docs/index.html` 한 파일에 모두 들어 있습니다. 외부 스크립트나 폰트를 쓰지 않고, 다크 모드도 자동 대응합니다. `docs/.nojekyll`은 GitHub이 파일을 그대로 서빙하도록 하는 표시라 지우지 마세요.

알림 문구나 명령어를 바꾸면 이 페이지의 예시도 같이 고쳐야 합니다.

## Railway 배포

저장소: [yuemyname/yongam-bot](https://github.com/yuemyname/yongam-bot)

1. Railway에서 **New Project → Deploy from GitHub repo**를 선택합니다.
2. `yuemyname/yongam-bot` 저장소를 연결합니다.
3. Volume을 추가하고 Mount Path를 `/data`로 설정합니다.
4. 서비스의 **Variables**에 다음 값을 등록합니다.

```dotenv
TELEGRAM_BOT_TOKEN=BotFather가_준_실제_토큰
TELEGRAM_CHAT_ID=운영자_chat_id
SUBSCRIPTIONS_ENABLED=true
NEW_SUBSCRIPTIONS_ENABLED=true
```

`TELEGRAM_CHAT_ID`는 최초 운영자를 기존 알림 구독자로 한 번 등록하고, 이후 `/statss` 응답과 CGV 조회 실패 공지를 받을 대상을 정합니다. 조회 실패 공지는 이 채팅에만 가며 구독자에게는 전달되지 않습니다. 이후 일반 사용자는 자신의 채팅에서 `/start`만 보내면 자동 등록됩니다. `/stop`으로 해지한 운영자는 재배포 후에도 자동으로 다시 등록되지 않습니다.

CGV 조회와 Telegram 발송은 서로 다른 작업에서 실행됩니다. 조회 작업은 알림을 `/data/notified.json`의 영속 발송 대기열에 기록한 즉시 다음 조회를 계속하고, 발송 작업은 기본 16개 작업자로 초당 25건까지 처리합니다. 따라서 구독자가 200~300명으로 늘어도 한 번의 대량 발송이 다음 CGV 조회 시작을 막지 않습니다. 신규 오픈은 좌석 변경과 운영 공지보다 높은 우선순위로 처리되며, 발송 작업은 최대 25건마다 우선순위를 다시 확인합니다.

이는 Telegram 무료 봇의 약 30건/초 대량 전송 한도 자체를 없애는 기능은 아닙니다. 300명에게 한 메시지를 보내는 데에는 이론상 약 12초가 필요하지만, 그 시간에도 CGV 조회는 계속됩니다. 필요하면 `TELEGRAM_BROADCAST_WORKERS`와 `TELEGRAM_BROADCAST_RATE_PER_SECOND`로 조정할 수 있지만, 초당 전송 수를 30보다 높게 설정하지 마세요.

구독자 목록과 설정, 처리한 Telegram 명령 위치, 기존 예매 알림 기록과 좌석 수·A열 제외 좌석 위치, 마지막 좌석 알림 시각, 실패한 일정 재조회 목록, 아직 보내지 못한 Telegram 알림은 `/data/notified.json`에 저장됩니다. Railway가 재시작되거나 새 버전을 배포해도 남은 알림부터 복구합니다.

> Telegram에 webhook이 설정된 봇은 `getUpdates` 방식과 동시에 사용할 수 없습니다. 이 프로젝트 전용 봇에는 별도 webhook을 설정하지 마세요.

### 신규 구독만 일시 중단하기

Railway Variables에서 `NEW_SUBSCRIPTIONS_ENABLED=false`로 설정하고 배포합니다.
`SUBSCRIPTIONS_ENABLED=true`는 유지하세요. 이 값을 false로 바꾸면 `/stop`을 포함한 Telegram 명령 처리가 모두 중단됩니다.

- 신규 사용자의 `/start`, `/subscribe`에는 중단 안내만 보내고 구독자를 추가하지 않습니다. 그룹과 `@봇이름`이 붙은 명령도 동일합니다.
- 기존 구독자·알림 설정·발송 대기열은 유지합니다. 기존 사용자는 설정 확인·변경·구독 해지가 가능합니다.
- `/help`, `/desc`, 미구독자의 `/status`에도 신규 구독 중단을 표시합니다.
- 해지 후 재구독도 제한됩니다. 운영자의 최초 1회 초기 등록 동작은 변경하지 않습니다.
- 재개하려면 `NEW_SUBSCRIPTIONS_ENABLED=true`로 되돌려 배포하세요. 구독자 상태 파일은 삭제하지 않습니다.
- 이 설정은 CGV 조회 중단·재개나 알림 발송 여부를 변경하지 않습니다.

### 신규 오픈 전용 운영

Railway Variables에서 `OPEN_ONLY_MODE=true`로 설정하고 배포합니다.

- 일정 API만 호출하고, 좌석 상세 API는 강제 재조회·회전 조회·보류 재조회까지 전부 건너뜁니다. 좌석 API 단일 진단 설정과 동시에 사용할 수 없습니다.
- 새 날짜·시작시간 회차만 알리고 기존 회차의 좌석 변화는 비교하지 않습니다. 매진이거나 일정에 좌석 수가 없어도 신규 회차 알림은 보냅니다.
- 메시지의 좌석 수는 일정 응답에 이미 포함된 값만 사용합니다. 좌석 행·번호는 조회하지 않습니다.
- 기존 오픈 중복 방지 기록과 개인 설정, 신규 구독 차단 상태는 유지합니다. '모두'·'신규 오픈만' 구독자는 오픈 알림을 받고, '잔여좌석만' 구독자는 `/mode_open` 선택 전까지 받지 않습니다. 상영일 필터는 그대로 적용합니다.
- 좌석 알림 대기열은 발송 작업자가 전송하지 않고 제거합니다. 오픈·운영 공지 대기열은 유지합니다. 이때 제거한 과거 좌석 알림은 다시 발송하지 않습니다.
- `/status`, `/help`, `/desc`, `/start`에 전용 운영을 표시합니다. 좌석·최소 좌석 수 변경 명령은 설정을 바꾸지 않고 중단 안내만 답합니다.
- `OPEN_ONLY_MODE=false`로 배포하면 기존 개인 설정으로 전체 기능이 복귀합니다. CGV 조회 간격·403 처리·중단 상태는 이 옵션으로 바뀌지 않습니다. 일정 API의 접근 제한도 해제하지 않습니다.

## Railway 로그 확인

정상 실행 시 다음과 비슷한 내용이 표시됩니다.

로그 시간은 `APP_TIMEZONE` 기준으로 표시되며 기본값은 한국시간(`Asia/Seoul`, `KST`)입니다.

```text
2026-08-14 00:54:36 KST INFO CGV Telegram Watcher 시작: ... 120초 간격
2026-08-14 00:54:39 KST INFO Telegram 구독자 변경: 현재 3명
2026-08-14 00:54:41 KST INFO ────────────────────────────────────────────────────────────
2026-08-14 00:54:41 KST INFO 조회 시작: 커서 모드, 15일 (2026-08-14~2026-08-28), 예매 열린 마지막 날 2026-08-25
2026-08-14 00:54:52 KST INFO 최우선 대기열: 신규 회차 알림 2개, 대상 3명
2026-08-14 00:54:52 KST INFO Telegram 대기열 발송: 처리 3건, 성공 3건, 실패 0건, 남음 0건
2026-08-14 00:55:10 KST INFO 제외: • 2026-08-16 22:00 — IMAX관 / 좌석 3/624석 잔여 좌석이 모두 A열입니다.
2026-08-14 00:55:11 KST INFO 좌석 대기열: • 2026-08-16 23:00 — IMAX관 / 좌석 9/624석 (9석), 대상 3명
2026-08-14 00:55:11 KST INFO 좌석 대기열: • 2026-08-16 23:30 — IMAX관 / 좌석 8/624석 (8석) ⚠️A열 여부 미확인, 대상 3명
2026-08-14 00:55:12 KST INFO 조회 완료: 성공 15일, 오류 0일, IMAX 회차 24개, 신규 2개, 예매 가능 좌석 1개, ...
```

`최우선 대기열:`과 `좌석 대기열:`은 발송할 알림이 안전하게 저장됐다는 뜻이고, `Telegram 대기열 발송:`은 실제 전송 결과입니다. 나머지 `제외:`·`보류:` 줄은 판정 결과이고, 회차 전체 상태는 `--verbose`의 `회차 판정` 줄에 있습니다. 좌석표에서 A열 여부를 읽지 못한 채 내보낸 알림은 대기열 줄 끝에 `⚠️A열 여부 미확인`이 붙습니다.

`HTTP 429`는 CGV가 요청을 일시적으로 제한한 상태입니다. 봇은 CGV 요청을 최소 2초 간격으로 하나씩 보내며, 첫 429가 나오면 해당 주기의 남은 일정·좌석 요청을 즉시 중단합니다. 실패하거나 생략된 일정 날짜는 상태 파일에 보존해 다음 정상 주기에 신규 오픈 후보 다음으로 우선 재조회합니다. 다음 조회는 30분 뒤로 미루고, 연속으로 발생하면 60분, 최대 120분까지 대기 시간을 늘립니다. 429 없이 조회가 끝나면 자동으로 2분 주기로 복귀합니다. 좌석 상세에서 A열 여부만 확인하지 못한 경우에는 전체 잔여 좌석이 6석 이하면 알림을 보류하고, 7석 이상이면 A열 여부를 확인하지 못했다는 표시와 함께 전체 잔여 수 기준으로 알림을 보냅니다.

`HTTP 403`은 CGV가 해당 조회 요청을 거부한 상태입니다. 첫 403에서 남은 일정·좌석 요청을 즉시 중단합니다. 아래 단일 재확인 모드를 사용하지 않는 일반 실행은 `FORBIDDEN_BACKOFF_SECONDS`(기본 1800초, 30분) 후 재시도하며, 429와 달리 연속 발생해도 늘리지 않고 같은 간격을 유지합니다. 403은 요청 속도가 아니라 출처 자체에 대한 판정이라 2분마다 두드리면 차단만 길어지기 때문입니다. 403 없이 조회가 끝나면 즉시 정상 주기로 복귀합니다. 오류 알림과 로그에는 차단 페이지 종류(CGV 자체 / Cloudflare), `Server` 헤더, `cf-ray` 유무를 함께 적어 어느 쪽이 거부했는지 구분할 수 있습니다. 봇의 대기 설정 변경이 CGV 측 차단 자체를 해제하는 것은 아닙니다.

정상 조회는 시작 시각 기준 120초 간격으로 반복합니다. 한 주기가 120초를 넘기면 끝난 뒤 약 0.5초 후 다음 주기를 시작합니다. 개별 CGV 요청은 이전 응답 완료 후 최소 2초를 기다리며, Telegram 발송과 명령 처리는 조회 대기 중에도 계속 실행됩니다.

### CGV 조회 1시간 중단 후 단일 재확인

Railway Variables의 `CGV_RECOVERY_REQUEST_ID`에 새로운 고유 이름(예: `recovery-20260920-1`)을 설정해 배포하면 해당 요청을 처음 읽은 시각부터 기본 1시간 동안 CGV 일정·좌석 요청을 모두 중단합니다. Telegram 명령과 기존 발송 대기열은 계속 처리합니다.

- 설정한 대기 후 신규 오픈 우선 조회 순서의 첫 날짜에 **HTTP 요청 1건만** 보냅니다. 연결 실패 재시도와 리디렉션 추적도 하지 않습니다.
- HTTP 200, 정상 JSON, `statusCode=0` 및 일정 데이터 구조를 확인하면 정상 조회로 복귀합니다. 회차가 없는 정상 응답도 성공입니다. 확인한 응답은 일반 알림 처리에 재사용해 중복 요청이나 신규 오픈 누락을 막습니다.
- 403뿐 아니라 429, 시간 초과, 비정상 JSON 등 **어떤 재확인 실패도 자동 조회 중단**으로 처리합니다. 복귀 후 다시 403이 발생해도 중단합니다. 운영자가 접근 제한·허용 여부를 확인하기 전까지 추가 CGV 요청을 보내지 않습니다.
- 중단·재확인 결과는 운영자에게만 Telegram으로 알립니다. 구독자는 `/status`에서 대기 예정 시각 또는 중단 상태를 확인할 수 있습니다.
- 예정 시각과 실행 상태는 볼륨의 상태 파일에 저장됩니다. 재시작·재배포·환경변수 삭제로 대기가 초기화되거나 중단이 해제되지 않습니다. 해제는 아래 `/cgv_resume` 명령 또는 새 요청 이름으로만 합니다. 요청 중 프로세스가 종료돼 결과가 불확실해도 재요청하지 않고 중단합니다.
- 다시 시험하려면 운영자 승인 후 **이전과 다른 요청 이름**으로 변경해 배포합니다. 기본적으로 새로 1시간을 기다립니다. 이미 중단된 상태에서 운영자가 **즉시 1건 확인**을 승인했다면 새 이름과 함께 `CGV_RECOVERY_PAUSE_SECONDS=0`을 설정합니다. 이 값은 0~86400초이며 기본 3600초입니다. 같은 요청 이름에서는 이 값을 바꿔도 저장된 예정 시각이나 중단 상태가 변경되지 않습니다.
- 즉시 확인 후에는 다음 요청의 실수를 막기 위해 대기 설정을 3600초로 돌려놓으세요. 같은 요청 이름은 재배포해도 재실행되지 않습니다. 상태 파일을 삭제하면 구독·중복 방지 기록도 없어지므로 삭제하지 마세요.

중단 중에는 새 예매 오픈·취소표를 감지할 수 없습니다. 복귀 시 기존 조회 범위를 다시 확인하지만 중단 중 나타났다 사라진 정보까지 복구할 수는 없습니다. 1시간 휴식이 CGV의 차단 해제를 보장하지는 않습니다.

### 운영자 명령으로 중단 해제 — `/cgv_resume`

`TELEGRAM_CHAT_ID` 운영자 계정에서 `/cgv_resume`를 보내면 재배포 없이 중단(또는 대기 중인 재확인)을 해제하고, 다음 주기부터 `POLL_INTERVAL_SECONDS` 간격의 일반 조회로 돌아갑니다. 자동 재확인 성공과 달리 **이후 403이 다시 나와도 자동 중단하지 않고** `FORBIDDEN_BACKOFF_SECONDS` 뒤 재시도합니다. 운영자가 차단 상태를 이미 확인하고 내린 판단이기 때문입니다.

```text
▶️ CGV 자동 조회를 재개합니다.
다음 주기부터 120초 간격으로 조회합니다. HTTP 403이 다시 나오면 자동 중단하지 않고 30분 뒤 재시도합니다.
중단 원인: 재확인 성공 후 정상 조회에서 HTTP 403이 다시 발생했습니다.
```

- 해제 상태도 상태 파일에 저장되므로 재시작·재배포 후에도 유지됩니다. 같은 `CGV_RECOVERY_REQUEST_ID`로는 새 대기가 시작되지 않고, 이름을 바꾸면 다시 1시간 대기 → 단일 재확인 흐름이 시작됩니다.
- 중단 상태가 아니면 아무것도 바꾸지 않고 그 사실만 답합니다.
- `CGV_HEADER_PROBE_REQUEST_ID`가 설정된 진단 모드에서는 매 주기 중단이 다시 걸리므로 해제할 수 없습니다. 변수를 지우고 재배포한 뒤 다시 보내야 합니다.
- 다른 사람이 보내면 일반 안내 문구가 나갑니다. **BotFather 명령어 목록에 등록하지 마세요.**

### 자동 조회를 재개하지 않는 헤더 단일 진단

운영자가 진단 1건을 명시적으로 승인한 경우에만 `CGV_HEADER_PROBE_REQUEST_ID`에 고유 ID,
`CGV_HEADER_PROBE_DATE`에 비교할 날짜(`YYYY-MM-DD`)를 함께 설정해 배포합니다.
일반 요청과 달리 `Sec-Fetch-Dest: empty`, `Sec-Fetch-Mode: cors`,
`Sec-Fetch-Site: same-origin` 세 헤더만 추가한 일정 요청 1건을 보냅니다.
개인 회원번호·로그인 쿠키는 사용하지 않으며 재시도·리디렉션 추적도 하지 않습니다.

**좌석 API를 검사할 때는** `CGV_HEADER_PROBE_SEAT_URL`에 인증정보를 제외한
`https://cgv.co.kr/api/v1/booking/searchIfSeatData?...` URL을 추가로 설정합니다.
허용 파라미터는 coCd, siteNo, scnYmd, scnsNo, scnSseq, seatAreaNo, cusgdCd뿐이며,
회원번호·토큰·중복 파라미터와 다른 호스트/경로는 거부합니다. 영화관·날짜는 설정과 같아야 합니다.
좌석 모드는 Referer를 인원 선택 페이지로, User-Agent와 Sec-CH-UA 계열을 캡처의 Chrome 153으로
맞추고 Priority와 위 Sec-Fetch 헤더를 보냅니다. Authorization·Cookie·custNo는 보내지 않습니다.
압축은 표준 라이브러리로 해제 가능한 gzip/deflate만 요청하고, 전송은 HTTP/1.1입니다.
브라우저 HTTP/2 연결 전체나 인증 세션을 재현하는 기능은 아닙니다.
로그의 endpoint·probe_kind·query로 **일정과 좌석 검사를 구분**할 수 있습니다.

- **성공해도 자동 조회를 재개하지 않습니다.** 중단 상태는 기존 상태 파일에 유지됩니다.
- 실행 전 볼륨의 `cgv-header-probes/`에 ID별 실행 기록을 배타적으로 생성하고 디스크에 동기화합니다.
  재시작·중복 배포·응답 전 종료 후에도 같은 ID를 다시 실행하지 않습니다. 기록을 삭제하지 마세요.
- 로그의 CGV_HEADER_PROBE_BEGIN(시작), CGV_HEADER_PROBE_RESULT(결과)로 실행 여부를 확인합니다.
  응답 본문·쿠키·로그인 토큰은 저장하지 않고 HTTP 상태, JSON 성공 여부, 소요 시간 등만 남깁니다.
- 구독자·중복 방지·미조회 날짜·알림 대기열은 진단 결과로 변경하지 않습니다.
  Telegram 명령과 기존 발송 작업자는 계속 동작합니다.
- 진단 변수를 제거해도 중단은 풀리지 않습니다. 정상 복귀는 변수를 제거하고 재배포한 뒤 운영자가 `/cgv_resume`를 보내거나 새 `CGV_RECOVERY_REQUEST_ID`를 설정해야 합니다.
- 정상 봇 요청의 헤더는 변경하지 않습니다. 단일 성공은 전체 조회나 장시간 운영의 성공을 보장하지 않습니다.

### 정상 일정 요청의 실제 전송 헤더 확인

운영자 승인 후 `CGV_WIRE_TRACE_REQUEST_ID`에 고유 ID를 지정하면 **다음 정상 일정 요청 1건**을
관찰합니다. 위 단일 진단과 달리 추가 요청이나 헤더 변경이 없고, 중단/재시도 상태도 바꾸지 않습니다.
조회가 중단된 상태에서는 이 설정만으로 요청이 실행되지 않습니다.

- 로그의 CGV_WIRE_TRACE 중 `headers_sent`는 `http.client`가 직렬화한 헤더를 TLS 전송 함수에 넘기고
  전송 함수가 정상 반환한 뒤 기록됩니다. 설정 딕셔너리만 출력하는 기능이 아닙니다.
- 같은 `request_id`의 `response`에 HTTP 상태와 Content-Type, Server, CF-Ray가 기록됩니다.
- 허용된 공개 헤더만 기록하며 Authorization·Cookie는 유무만, custNo는 존재 여부만 표시합니다.
  Referer 쿼리/fragment, 알 수 없는 요청 쿼리와 헤더 값, 응답 본문은 기록하지 않습니다.
- `/data/cgv-wire-traces/`(STATE_FILE 디렉터리 기준)에 실행 기록을 남겨 같은 ID는 재시작해도
  반복하지 않습니다. 전송 실패도 실행 1회로 취급합니다. 기존 조회의 재시도 정책은 유지됩니다.
- **클라이언트 전송 증거이지 CGV가 수신한 헤더를 서버에서 직접 확인한 증거는 아닙니다.**
  진단 후 ID를 비워 다음 배포부터 비활성화할 수 있습니다. 기록 파일은 삭제하지 마세요.

## Mac에서 직접 실행

### 운영자 승인 공개 헤더 비교

`CGV_PUBLIC_MATRIX_REQUEST_ID`와 `CGV_PUBLIC_MATRIX_BASE_DATE`,
`CGV_PUBLIC_MATRIX_COMPARE_DATE`(각 YYYY-MM-DD)를 지정하면 정상 실행 시작 시 최대 14건을
30초 이상 간격으로 비교합니다. 날짜, 압축, Chrome 버전, Priority, Sec-CH 3종, Sec-Fetch 3종의
개별 변경과 전체 조합, 마지막 기준 재확인을 포함합니다. 대상은 용산 IMAX 오디세이 일정 API뿐입니다.
Authorization·Cookie·custNo는 어떤 경우에도 추가하지 않으며 HTTP/1.1/TLS 전송 방식을 유지합니다.

- CGV_PUBLIC_MATRIX_RESULT 로그에 HTTP 결과, 압축/JSON 해석 여부, 공개 진단값만 남깁니다.
  전송 헤더는 CGV_WIRE_TRACE 로그에서 같은 ID와 case로 연결됩니다. 본문·쿠키는 저장하지 않습니다.
- 압축 헤더는 브라우저와 같은 값을 시험하지만 br/zstd 응답은 해석 미지원으로 표시합니다.
  지원하지 않는 응답을 정상 JSON으로 간주하지 않습니다.
- 403은 승인된 비교 범위 내에서만 계속하며, 429·Retry-After·challenge·요청 오류 시 즉시 종료합니다.
  재시도/리디렉션은 없고 볼륨의 `cgv-public-matrices/` 실행 기록으로 같은 ID의 반복을 막습니다.
- 진단 동안 정규 CGV 조회는 겹치지 않고 Telegram 명령/발송 작업자는 계속 실행됩니다.
  진단 결과로 구독자·감지/알림 기록·복구 상태를 변경하지 않습니다.
- 완료 후 최소 30분(기존 403/초기 429 설정 또는 Retry-After가 더 크면 그 시간) 대기한 뒤 기존 조회 흐름으로
  돌아갑니다. 원래 중단 상태이면 중단이 유지됩니다. 재시작으로 진단을 다시 실행하지 않습니다.
- `--dry-run`, `--once`에서는 자동 실행하지 않습니다. 로컬 독립 진단은
  `python3 cgv_public_matrix.py --request-id <고유ID> --state-dir <진단기록폴더> --base-date YYYY-MM-DD --compare-date YYYY-MM-DD`
  로 실행합니다. 이 명령은 .env나 Telegram 인증값을 읽지 않습니다.

### 실행 순서

1. `setup.command`를 실행해 `.env`를 만듭니다.
2. `TELEGRAM_BOT_TOKEN`과 최초 운영자의 `TELEGRAM_CHAT_ID`를 입력합니다.
3. `test_telegram.command`로 Telegram 전송을 시험합니다.
4. `run.command`를 실행하거나 `install_background.command`로 로그인 시 자동 실행을 설치합니다.

Mac이 잠자기 상태이거나 덮개가 닫혀 있으면 감시가 중단될 수 있으므로 여러 구독자가 사용하는 봇은 Railway 운영을 권장합니다.

## 주요 설정값

| 이름 | 기본값 | 설명 |
|---|---:|---|
| `SUBSCRIPTIONS_ENABLED` | `true` | Telegram 명령 처리 전체 활성화 (`/stop` 포함) |
| `NEW_SUBSCRIPTIONS_ENABLED` | `true` | 신규 구독 허용. false여도 기존 구독자 명령은 유지 |
| `OPEN_ONLY_MODE` | `false` | 일정 API만 조회해 신규 오픈만 알림. 좌석 API·좌석 알림 대기열 발송 금지 |
| `POLL_INTERVAL_SECONDS` | `120` | 정상 조회 주기 |
| `FORBIDDEN_BACKOFF_SECONDS` | `1800` | HTTP 403 차단 후 재시도 간격(30분). 연속 발생해도 늘리지 않음 |
| `CGV_RECOVERY_REQUEST_ID` | 빈 값 | 운영자 승인 단일 재확인 요청 이름: 1시간 중단 → 1건 확인 → 성공 시 복귀, 실패 시 중단 |
| `CGV_RECOVERY_PAUSE_SECONDS` | `3600` | 새 재확인 요청의 대기 시간(초). 운영자가 즉시 재확인을 승인한 경우만 0 사용 |
| `CGV_HEADER_PROBE_REQUEST_ID` | 빈 값 | 운영자 승인 헤더 단일 진단 ID. 성공해도 자동 조회 중단 유지 |
| `CGV_HEADER_PROBE_DATE` | 빈 값 | 헤더 진단의 명시적 상영일(YYYY-MM-DD) |
| `CGV_HEADER_PROBE_SEAT_URL` | 빈 값 | 익명 좌석 API 단일 검사 URL. 비어 있으면 일정 API 검사 |
| `CGV_WIRE_TRACE_REQUEST_ID` | 빈 값 | 기존 일정 요청 1건의 직렬화된 실제 전송 헤더만 관찰하는 진단 ID |
| `TELEGRAM_COMMAND_POLL_SECONDS` | `2` | CGV 조회 중·대기 중 Telegram 명령 확인 주기 |
| `CGV_REQUEST_SPACING_SECONDS` | `2` | 연속 CGV 요청 사이의 최소 간격 |
| `RATE_LIMIT_BACKOFF_INITIAL_SECONDS` | `1800` | HTTP 429 발생 후 첫 대기 시간(30분) |
| `RATE_LIMIT_BACKOFF_MAX_SECONDS` | `7200` | 연속 HTTP 429 시 최대 대기 시간(120분) |
| `DYNAMIC_DATE_WINDOW` | `true` | 오늘 기준 감시 범위를 매일 이동 |
| `TARGET_WINDOW_DAYS` | `28` | 오늘을 포함해 감시할 날짜 수 |
| `APP_TIMEZONE` | `Asia/Seoul` | 날짜 계산 기준 시간대 |
| `STRICT_IMAX_MATCH` | `true` | IMAX 이름 또는 코드가 있는 회차만 감지 |
| `BOOKING_CLOSE_MARGIN_MINUTES` | `0` | 상영 시작 몇 분 전부터 예매 마감으로 볼지 |
| `DEFERRED_RECHECK_CYCLES` | `5` | 보류된 회차를 몇 주기마다 다시 조회할지 |
| `SEAT_ALERT_REPEAT_MINUTES` | `0` | 좌석 수가 그대로인 회차를 몇 분마다 다시 알릴지 (0 = 매 주기) |
| `SEAT_RECHECK_ALWAYS_DAYS` | `2` | 잔여 수가 그대로여도 좌석표를 매 주기 다시 읽을 일수 |
| `SEAT_RECHECK_ROTATE_DAYS` | `7` | 그 뒤로 순환하며 다시 읽을 일수 |
| `SEAT_RECHECK_ROTATE_CYCLES` | `5` | 순환 구간을 몇 주기에 걸쳐 한 바퀴 돌지 |
| `PENDING_DELIVERY_MAX_ATTEMPTS` | `30` | 전송 실패 재시도를 몇 번까지 할지 |
| `SCAN_MODE` | `cursor` | 신규 오픈 후보를 먼저 보는 조회 방식 (`full`로 전체 조회 가능) |
| `CURSOR_PROBE_DAYS` | `3` | `cursor` 모드에서 프론티어 뒤로 더 볼 일수 |
| `CURSOR_EXPANSION_DAYS` | `21` | 신규 오픈 감지 후 이어서 조회할 최대 일수(안전장치) |
| `FULL_SCAN_EVERY_CYCLES` | `10` | `cursor` 모드에서도 전체를 조회하는 주기 |

`DEFERRED_RECHECK_CYCLES`는 이미 예매 오픈 알림을 보낸 회차의 좌석 상세 판별이 실패했을 때만 적용됩니다. 신규 회차는 좌석 상세를 아예 조회하지 않고 오픈 알림을 먼저 보내므로 해당되지 않습니다. 잔여 좌석 수가 바뀐 경우에는 설정된 대기 횟수와 관계없이 즉시 다시 확인합니다.

### 잔여 수가 그대로일 때의 좌석 재확인

상영 일정 응답에는 전체 잔여 좌석 수만 들어 있습니다. 그래서 A열 1석이 팔리는 동시에 A열이 아닌 1석이 취소되면 전체 수가 9석 그대로여서, 잔여 수만 비교해서는 좌석 구성이 바뀐 것을 알 수 없습니다. `SEAT_RECHECK_*` 세 값은 잔여 수가 그대로여도 좌석표를 다시 읽을 날짜를 정합니다.

기본값(`2` / `7` / `5`)에서 주기별로 다시 읽는 날짜는 다음과 같습니다. `●`가 강제로 좌석표를 다시 읽는 날짜입니다.

```text
주기 | +0 +1 +2 +3 +4 +5 +6 +7 …
  0  | ●  ●  ·  ·  ·  ·  ●  ·
  1  | ●  ●  ●  ·  ·  ·  ·  ·
  2  | ●  ●  ·  ●  ·  ·  ·  ·
  3  | ●  ●  ·  ·  ●  ·  ·  ·
  4  | ●  ●  ·  ·  ·  ●  ·  ·
  5  | ●  ●  ·  ·  ·  ·  ●  ·   ← 5주기마다 한 바퀴
```

오늘·내일은 매 주기, 그 뒤 `+2`~`+6`은 5주기에 한 번씩 돌아옵니다. 주기당 3일치가 늘어나므로 하루 5회차 기준 좌석 상세 요청이 15건, `CGV_REQUEST_SPACING_SECONDS=2` 기준 주기가 약 30초 길어집니다. `+7` 이후 날짜는 잔여 수가 바뀔 때만 좌석표를 읽습니다. 세 값 중 앞의 두 개를 `0`으로 두면 이 기능이 꺼져, 잔여 수가 바뀐 날짜만 좌석표를 다시 읽습니다. 알림 발송 자체는 이 값과 무관하며, 좌석이 있으면 저장된 좌석표를 기준으로 계속 나갑니다.

순환 슬롯은 날짜 자체로 정해지므로 봇을 재시작해도 특정 날짜만 계속 밀리는 일이 없습니다.

## 전송 실패 처리

Telegram 전송이 실패하면 원인에 따라 다르게 처리합니다.

| 응답 | 처리 |
|---|---|
| `403 bot was blocked by the user` | **구독 목록에서 즉시 제거**, 대기 중인 재전송도 함께 삭제 |
| `403 user is deactivated` / `bot was kicked` | 동일 |
| `chat not found` | 동일 |
| `429`, `5xx`, 연결 실패 등 | 그 사람의 항목만 대기열에 남겨 자동 재전송 |

봇을 차단한 사람은 `/stop`을 보낼 수 없어 스스로 해지할 수 없습니다. 이 경우를 구분하지 않으면 재전송 대기열이 계속 쌓이고 매 주기 헛된 요청이 늘어나므로, 영구 실패는 재시도 없이 구독을 정리합니다.

일시적 실패는 2초부터 최대 5분까지 재시도 간격을 늘리며 `PENDING_DELIVERY_MAX_ATTEMPTS`회까지 재시도한 뒤 포기합니다. 감시기가 오래 멈춰 있어 시도 횟수가 늘지 않는 경우를 대비해, 대기열에 들어간 지 24시간이 지난 항목은 횟수와 관계없이 정리합니다.

```text
INFO 구독 해지: chat_id=... 에 더 이상 보낼 수 없어 목록에서 제거했습니다.
INFO 재전송 대기 3건이 24시간을 넘겨 정리했습니다.
```

### Telegram 명령 조회가 실패할 때

`getUpdates`가 실패하면 그 주기의 명령 확인만 건너뜁니다. **처리 위치(offset)를 올리지 않으므로 대기 중이던 명령은 Telegram에 그대로 남아 있고**, 다음 확인 때 다시 받아옵니다. 유실은 없습니다.

실패가 이어지면 확인 간격을 2초에서 두 배씩 늘려 최대 60초까지 벌립니다. 로그는 장애 시작에 WARNING 한 줄, 복구에 INFO 한 줄만 남깁니다.

```text
WARNING Telegram 구독 명령 조회 오류: HTTP 502 - Bad Gateway
INFO Telegram 명령 조회 복구: 연속 5회 실패 후 정상화
```

1시간짜리 Telegram 장애 기준으로 요청 1800건·로그 1800줄이 **요청 63건·로그 1줄**로 줄어듭니다. 이미 실패하는 API를 2초마다 두드려봐야 얻는 것이 없고, 그러다 봇이 따로 요청 제한을 받을 수 있습니다.

## 조회 범위 — full과 cursor

`SCAN_MODE=full`은 매 주기 감시 기간 28일을 모두 조회합니다. `CGV_REQUEST_SPACING_SECONDS=2` 때문에 한 주기에 최소 약 56초가 걸리고, 좌석 상세 조회가 붙으면 `POLL_INTERVAL_SECONDS`를 넘겨 사실상 쉬는 시간 없이 돌아갑니다.

기본값인 `SCAN_MODE=cursor`는 조회 범위를 좁히고 신규 오픈을 우선합니다. 상태 파일에 **프론티어** — IMAX 회차가 관측된 가장 늦은 상영일 — 를 저장하고, 매 주기 다음 순서로 조회합니다.

```
[프론티어+1 .. +3]        커서 (신규 오픈 감지용)
[오늘 .. 프론티어]        이미 열린 구간 (가까운 상영일부터)
```

예를 들어 마지막 예매 가능일이 8월 20일이면 `8/21 → 8/22 → 8/23 → 오늘 → 내일 → … → 8/20` 순서로 일정 API를 요청합니다.

커서 구간에서 회차가 잡히면 같은 주기 안에서 **회차가 없는 날짜가 나올 때까지** 하루씩 이어서 조회합니다. 예매는 연속된 구간으로 열리므로 첫 빈 날짜가 곧 새로 열린 범위의 끝입니다. 빈 날짜 하나를 조회해야 끝을 알 수 있으므로 항상 하루를 더 봅니다. 그 뒤 바로 오늘부터의 취소표 확인으로 넘어갑니다. `CURSOR_EXPANSION_DAYS`는 이 조회가 한 주기에 넘지 않을 최대 일수를 정하는 안전장치입니다.

예매가 13일 앞까지 열려 있을 때 주기당 요청은 28건에서 **17건**으로 줄어듭니다. 절감폭은 예매가 얼마나 앞까지 열려 있는지에 비례해 달라집니다.

**누락 방지 장치**

- `FULL_SCAN_EVERY_CYCLES` 주기마다 한 번은 전체를 조회합니다. 커서가 놓친 것이 있어도 최대 그 주기 안에 잡힙니다
- 상태 파일에 프론티어가 없으면(최초 실행, 볼륨 초기화) 전체를 조회합니다
- 프론티어는 **전진만** 합니다. HTTP 429로 조회가 중간에 끊겨도 프론티어가 뒤로 밀리지 않습니다
- 실패했거나 429 뒤에 생략된 날짜는 상태 파일에 남겨 다음 정상 주기에 우선 재조회합니다
- 신규 오픈 후보를 주기 맨 앞에서 확인하므로, 그 뒤 날짜별로 좌석 상세까지 끝내도 오픈 감지가 늦어지지 않습니다

**알려진 한계**

프론티어와 다음 오픈 날짜 사이에 `CURSOR_PROBE_DAYS`보다 큰 공백이 있으면 커서가 넘지 못하고, 다음 전체 조회 때까지 감지가 늦어집니다. 기본값 기준 최대 10주기입니다. `CURSOR_PROBE_DAYS`를 늘리면 줄어들지만 그만큼 요청이 늘어납니다.

**전환 방법**

매 주기 로그에 조회 범위가 남습니다.

```text
조회 시작: 전체 모드, 28일 (2026-08-13~2026-09-09)
조회 시작: 커서 모드, 17일 (2026-08-13~2026-08-29)
```

기본값 그대로 `cursor`로 운영하면 됩니다. 전체 28일을 매 주기 모두 확인해야 할 때만 Railway 환경변수에 `SCAN_MODE=full`을 넣으세요. `cursor` 모드도 `FULL_SCAN_EVERY_CYCLES` 주기마다 전체 범위를 확인합니다.

## 구독 현황 확인 — `/statss`, `/statsss`

`.env`의 `TELEGRAM_CHAT_ID`로 지정된 운영자 계정에서만 응답합니다. `/statss`는 집계와 구독자 목록을 모두 보여주고, `/statsss`는 구독자 목록을 제외한 집계만 보여줍니다. 다른 사람이 보내면 일반 안내 문구가 나가므로, 명령이 있다는 사실 자체가 구독자에게 드러나지 않습니다. **둘 다 BotFather 명령어 목록에 등록하지 마세요.**

```text
📊 구독 현황
전체 11명
채팅 유형: 개인 8명, 그룹 2명, 미상 1명

알림 종류
• 신규 오픈 + 잔여 좌석 — 8명
• 신규 오픈만 — 2명
• 잔여 좌석만 — 1명

상영일
• 모든 요일 상영분 — 9명
• 주말(토·일) 상영분만 — 2명

잔여 좌석 대상
• 모든 A열 제외 좌석 — 9명
• 명당 좌석만 — 2명

예매 가능 최소 좌석
• 1석부터 모두 — 10명
• 2석 이상 남았을 때만 — 1명

📋 구독자 목록
1. 7061234567 · 홍길동 · 개인 · 08-19
   신규 오픈 + 잔여 좌석 · 모든 요일 상영분 · 모든 A열 제외 좌석 · 1석부터 모두
2. -1001234567890 · 용아맥 대기방 · 그룹 · 08-16
   신규 오픈만 · 주말(토·일) 상영분만 · 모든 A열 제외 좌석 · 1석부터 모두
```

설정한 적 없는 구독자는 기본값(`신규 오픈 + 잔여 좌석`, `모든 요일 상영분`, `모든 A열 제외 좌석`, `1석부터 모두`)으로 집계됩니다.

구독자마다 chat_id, 표시 이름, 채팅 유형, 구독일, 설정 네 가지가 나옵니다. 목록은 `subscribed_at`을 기준으로 오래된 구독자부터 정렬하며, 구독일을 알 수 없는 오래된 기록은 맨 아래에 표시합니다. Telegram이 4096자를 넘는 메시지를 거부하므로 목록이 길어지면 들어가는 만큼만 싣고 `… 외 N명`으로 끊습니다. 글자 수 기준이라 이름이 긴 그룹이 섞여 있어도 메시지가 통째로 실패하지 않습니다.

같은 정보를 Railway **Console**에서 직접 볼 수도 있습니다.

```bash
python3 -c "import json;d=json.load(open('/data/notified.json'));[print(k,v) for k,v in d['subscribers'].items()]"
```

## 구독자 메시지 받기

구독자가 명령어가 아닌 일반 문장을 보내면 운영자에게 전달됩니다. 별도 설정은 없습니다.

```text
💬 구독자 메시지
7061234567 · 홍길동
━━━━━━━━━━━━━━━━━━━━
알림 너무 많이 와요 ㅠㅠ 줄이는 방법 있나요?
```

구독하지 않은 사람이 보낸 경우 `· 구독 안 함`이 붙습니다. 봇이 공개돼 있어 누구나 말을 걸 수 있기 때문에, **한 채팅당 시간당 5건까지만** 전달하고 500자를 넘으면 뒷부분을 자릅니다. 상한에 걸리면 로그에만 남습니다.

```text
INFO 메시지 전달 생략: chat_id=... 가 1시간 안에 5건을 넘겼습니다.
```

보낸 사람에게는 전달했다는 짧은 확인이 갑니다. 아무 반응이 없으면 봇이 고장 난 것처럼 보이기 때문입니다.

명령어(`/`로 시작)는 평소대로 처리되고 전달되지 않습니다. 운영자 자신이 보낸 메시지도 전달하지 않습니다.

### 답장하기 — `/reply`

전달된 메시지 첫 줄의 `chat_id`를 그대로 쓰면 됩니다.

```text
/reply 7061234567 /count_2 로 바꾸시면 2석 이상 남았을 때만 알림이 가요!
```

받는 사람에게는 `💬 운영자 답장` 머리말과 함께 전달되고, 운영자에게는 누구에게 갔는지 확인이 돌아옵니다.

```text
✅ 답장을 보냈습니다.
7061234567 · 홍길동
```

`chat_id`를 잘못 적으면 엉뚱한 사람에게 갑니다. 그래서 숫자가 아니면 보내기 전에 거르고, 최근에 메시지를 보낸 적도 구독 중도 아닌 상대에게 보내면 `⚠️ 모르는 대상`을 붙여 알려줍니다. 보낸 뒤에 바로 확인할 수 있습니다.

상대가 봇을 차단했으면 전송이 실패하고 구독 목록에서 정리합니다.

전원 공지는 `/notice`를 쓰세요.

## 구독자 전원에게 공지 — `/notice`

운영자 계정에서만 응답합니다. **BotFather 명령어 목록에 등록하지 마세요.**

한 번 나가면 되돌릴 수 없으므로 두 단계로 나눠져 있습니다.

```text
/notice 알림이 너무 자주 오면 아래처럼 줄일 수 있어요.
• /count_2 — 2석 이상 남았을 때만 받기
• /seat_sweet — 중앙 명당 구역에 자리가 있을 때만 받기
지금 설정은 /status 로 확인할 수 있어요.
```

먼저 미리보기와 수신 대상 인원이 돌아옵니다. 이 시점까지는 아무에게도 가지 않습니다.

```text
미리보기 — 아직 아무에게도 가지 않았습니다.
━━━━━━━━━━━━━━━━━━━━
📢 공지

알림이 너무 자주 오면 아래처럼 줄일 수 있어요.
...
━━━━━━━━━━━━━━━━━━━━
수신 대상 11명

보내려면 /notice_send
고치려면 /notice 로 다시 작성
10분 안에 보내지 않으면 취소됩니다.
```

`/notice_send`를 보내야 실제로 전송됩니다. 초안은 메모리에만 있어서 재배포하면 사라지고, 10분이 지나도 취소됩니다. 같은 초안을 두 번 보낼 수 없습니다.

공지는 `/mode`·`/seat`·`/count` 설정과 무관하게 **구독자 전원**에게 갑니다. 알림이 아니라 사람이 쓴 안내이기 때문입니다. `📢 공지` 머리말이 붙어 알림과 구분됩니다.

내용 없이 `/notice`만 보내면 사용법과 함께 복사해서 쓸 수 있는 예시 문구가 나옵니다.

> 알림이 잦다는 얘기가 여러 명에게서 나온다면 공지보다 `SEAT_ALERT_REPEAT_MINUTES`를 올리는 쪽이 근본적인 해결입니다. 기본값이 안 맞는다는 신호이기 때문입니다.

## 회차별 판정 확인

평소 로그(INFO)는 **알림을 보낸 회차**와 **그 주기에 제외·보류 상태로 들어간 회차**만 남깁니다. 이미 매진이거나 A열만 남은 채로 계속 머무는 회차는 매 주기 다시 찍지 않습니다. 회차 수십 개가 매분 같은 줄을 쌓는 것을 막기 위해서입니다.

그래서 INFO는 "이번 주기에 무엇이 바뀌었나"를, DEBUG 판정 줄은 "지금 각 회차가 어떤 상태인가"를 보여줍니다. 특정 회차가 왜 알림이 안 오는지 확인하려면 `--verbose`로 실행하세요. 주기 끝에 그 주기에 본 모든 회차의 판정이 **한 줄로** 나옵니다. Railway 배포는 `railway.toml`의 `startCommand`에 이미 `--verbose`가 들어 있습니다. 회차 55개 기준 한 줄이 약 2KB이고 하루 약 2.6MB입니다. 로그가 부담되면 `startCommand`에서 `--verbose`를 빼세요.

```text
DEBUG 회차 판정 5건 | 08-26 07:00 400/624 제외·예매 마감 | 08-26 14:00 0/624 제외·매진 | 08-26 17:00 3/624 제외·A열만 | 08-26 20:00 5/624 보류·A열 여부 미확인 | 08-26 23:00 9/624 발송·예매 가능 9석
```

판정 문구는 다음 중 하나입니다.

| 판정 | 뜻 |
|---|---|
| `발송·예매 오픈` | 처음 보는 상영일·시각이라 오픈 알림 발송 (매진이어도) |
| `발송·예매 가능 9석` | 예매 가능한 좌석이 있어 좌석 알림 발송 |
| `오픈 알림으로 갈음` | 같은 주기에 오픈 알림을 보내 좌석 알림은 생략 |
| `대기·재알림 간격` | 좌석은 있지만 `SEAT_ALERT_REPEAT_MINUTES`가 아직 안 지남 |
| `제외·예매 마감` | 상영이 이미 시작됨 |
| `제외·매진` | 잔여 0석 |
| `제외·A열만` | 판매 가능한 좌석이 모두 A열 |
| `보류·A열 여부 미확인` | 좌석표에서 A열 여부를 못 읽었고 잔여 6석 이하 |

## 개발 및 테스트

별도 Python 패키지 설치 없이 Python 3.10 이상에서 실행됩니다.

```bash
# 한 번 조회
python3 watcher.py --once

# Telegram 전송과 상태 저장 없이 확인
python3 watcher.py --once --dry-run

# 자동 테스트
python3 -m unittest discover -s tests -v
```

실제 `.env`, Bot Token, 구독자 상태 파일과 로그는 Git에 포함하지 마세요.
